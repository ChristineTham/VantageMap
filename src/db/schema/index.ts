/**
 * Database schema — Phase 3 will add all entity tables here.
 *
 * Each bounded context gets its own file under src/db/schema/:
 *   business.ts      — BusinessCapability, Organization, BusinessContext
 *   applications.ts  — Application, DataObject, Interface
 *   strategy.ts      — StrategicObjective, Initiative, Platform
 *   technology.ts    — TechCategory, ITComponent, Provider
 *   relationships.ts — generic edge table
 *   tags.ts          — Tag, Subscription, custom fields
 *   audit.ts         — AuditEntry
 *   users.ts         — User, Role, Workspace, UserWorkspaceRole (Better Auth)
 *
 * Re-export everything from this barrel so src/db/index.ts stays clean.
 */

// Phase 3 schema files will be imported and re-exported here.
// Example: export * from "./business";

// Placeholder export so this file is treated as a module.
export const __placeholder = {};
